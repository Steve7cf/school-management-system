<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Login | School Management System</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" rel="stylesheet">
  <style>
    html, body {
      height: 100%;
    }
    body {
      min-height: 100vh;
      height: 100%;
      background: linear-gradient(135deg, #5D3FD3 0%, #48D1CC 100%) !important;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      margin: 0;
    }
    .admin-login-wrapper {
      flex: 1 0 auto;
      display: flex;
      align-items: center;
      justify-content: center;
      width: 100%;
    }
    .admin-login-card {
      border: none;
      border-radius: 1.5rem;
      box-shadow: 0 8px 32px rgba(93,63,211,0.18), 0 1.5px 8px rgba(72,209,204,0.08);
      overflow: hidden;
      background: #fff;
      padding: 2.5rem 2rem 2rem 2rem;
      max-width: 420px;
      margin: 2rem auto;
    }
    .admin-login-header {
      background: linear-gradient(90deg, #5D3FD3 60%, #48D1CC 100%);
      color: #fff;
      padding: 2rem 1rem 1.5rem 1rem;
      border-radius: 0 0 2rem 2rem;
      text-align: center;
      margin: -2.5rem -2rem 2rem -2rem;
      box-shadow: 0 2px 12px rgba(93,63,211,0.10);
    }
    .admin-login-header i {
      font-size: 2.5rem;
      margin-bottom: 0.5rem;
      color: #fff;
      background: rgba(255,255,255,0.10);
      border-radius: 50%;
      padding: 0.7rem;
      box-shadow: 0 2px 8px rgba(93,63,211,0.10);
    }
    .admin-login-header h3 {
      font-weight: 800;
      letter-spacing: 1px;
      margin-bottom: 0.25rem;
    }
    .admin-login-header p {
      font-size: 1.1rem;
      opacity: 0.85;
      margin-bottom: 0;
    }
    .admin-login-card .form-label {
      font-weight: 600;
      color: #5D3FD3;
    }
    .admin-login-card .form-control {
      border-radius: 0.75rem;
      border: 1.5px solid #e0e0e0;
      font-size: 1.05rem;
      padding: 0.75rem 1rem;
      background: #f8f8ff;
      color: #333;
    }
    .admin-login-card .form-control:focus {
      border-color: #5D3FD3;
      box-shadow: 0 0 0 0.15rem rgba(93,63,211,0.10);
      background: #fff;
    }
    .admin-login-card .btn-primary {
      background: linear-gradient(90deg, #5D3FD3 60%, #48D1CC 100%);
      border: none;
      border-radius: 0.75rem;
      font-weight: 700;
      font-size: 1.1rem;
      padding: 0.75rem 0;
      box-shadow: 0 2px 8px rgba(93,63,211,0.10);
      transition: background 0.2s, box-shadow 0.2s;
    }
    .admin-login-card .btn-primary:hover {
      background: linear-gradient(90deg, #4a32a8 60%, #3bb8b3 100%);
      box-shadow: 0 4px 16px rgba(93,63,211,0.18);
    }
    .admin-login-card .alert {
      border-radius: 0.75rem;
      font-size: 1rem;
      margin-bottom: 1.25rem;
    }
    .admin-login-footer {
      flex-shrink: 0;
      width: 100%;
      text-align: center;
      color: #fff;
      opacity: 0.85;
      font-size: 0.98rem;
      letter-spacing: 0.5px;
      margin-top: auto;
      padding: 1.5rem 0 1rem 0;
    }
  </style>
</head>
<body>
  <div class="admin-login-wrapper">
    <div class="admin-login-card">
      <div class="admin-login-header">
        <i class="fas fa-user-shield"></i>
        <h3>Admin Login</h3>
        <p>School Management System</p>
      </div>
      <% if (info && info.length > 0) { %>
        <div class="alert alert-danger">
          <i class="fas fa-exclamation-triangle me-2"></i>
          <%= info[0] %>
        </div>
      <% } %>
      <form action="/login/admin" method="post" autocomplete="off">
        <div class="mb-3">
          <label for="email" class="form-label">Email address</label>
          <input type="email" class="form-control" id="email" name="email" required autofocus>
        </div>
        <div class="mb-3">
          <label for="password" class="form-label">Password</label>
          <input type="password" class="form-control" id="password" name="password" required>
        </div>
        <button type="submit" class="btn btn-primary w-100">Login</button>
      </form>
      <div class="mt-4 text-center">
        <span class="text-muted">Don't have an admin account?</span>
        <a href="/admin/signup" class="text-primary fw-semibold">Sign up</a>
      </div>
    </div>
  </div>
  <div class="admin-login-footer">
    &copy; <%= new Date().getFullYear() %> Full Stack School. All rights reserved.
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 